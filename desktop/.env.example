# Google Gemini API Configuration (Primary)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenRouter API Configuration (Fallback)
# Get your API key from: https://openrouter.ai/keys
# Provides access to Qwen, DeepSeek, and other models when Gemini is unavailable
# Optional - leave empty if you only want to use Gemini
OPENROUTER_API_KEY=

# PII Detection Feature Flags
# Set to "true" to enable the new integrated detector (recommended)
USE_NEW_PII_DETECTOR=true

# Optional: Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
